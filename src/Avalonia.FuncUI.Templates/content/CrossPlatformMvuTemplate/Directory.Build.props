<Project>
  <PropertyGroup>
    <!-- #if (Platform == iOS || Platform == Android) -->
    <!-- Application information for mobile projects -->
    <ApplicationId>AvaIdParameter</ApplicationId>
    <ApplicationVersion>AvaVersionParameter</ApplicationVersion>
    <ApplicationDisplayVersion>AvaDisplayVersionParameter</ApplicationDisplayVersion>
    <ApplicationTitle>AvaTitleParameter</ApplicationTitle>
    <!-- #endif -->
    <!-- Avalonia versions -->
    <AvaloniaVersion>11.0.0</AvaloniaVersion>
    <AvaloniaFuncUIVersion>1.0.0</AvaloniaFuncUIVersion>
  </PropertyGroup>
</Project>