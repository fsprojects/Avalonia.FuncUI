name: Build and Test

on:
  pull_request:
    branches: [ main, master ]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  build:

    runs-on: macos-15

    steps:
    - name: xCode
      run: sudo xcode-select -s /Applications/Xcode_26.3.app/Contents/Developer/

    - uses: actions/checkout@v6

    - name: Setup .NET
      uses: actions/setup-dotnet@v5
      with:
        dotnet-version: |
          10.x.x

    - name: Install workloads
      run: dotnet workload restore src/Avalonia.FuncUI.slnx

    - name: Restore dependencies
      run: dotnet restore src/Avalonia.FuncUI.slnx

    - name: Build
      run: dotnet build src/Avalonia.FuncUI.slnx --no-restore

    - name: Test
      run: dotnet test src/Avalonia.FuncUI.slnx --no-build --verbosity normal
